using UnityEngine;

// This is basically a Module version of LLMAsyncPlanDriver
//
// It handles sending a request to the LLM,
//   receiving the response,
//   converting response into tasks or reaction scripts.
//
// It has a central helper that routes requests to the LLM,
//   and directs the responses to the Agent that requested it.

public class LLMRequestResponseModule : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
