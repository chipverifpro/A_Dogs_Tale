using UnityEngine;

// This maintains the Task List for the agent.  It has functions
// to add/remove tasks, but is mainly just the list for other
// modules to pass tasks into.

public class TaskListModule : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
